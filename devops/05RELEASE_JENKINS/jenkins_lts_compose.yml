services:
  jenkins:
    image: docker.io/library/jenkins/jenkins:lts
    privileged: true
    user: root
    ports:
      - "8081:8080"
      - "50000:50000"
    volumes:
      - ${VOLUME_DIR}/jenkins_data:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock

